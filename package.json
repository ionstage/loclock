{
  "name": "loclock",
  "version": "0.1.1",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/*.css, js/*.js' --server"
  },
  "devDependencies": {
    "browser-sync": "*"
  },
  "private": true
}
